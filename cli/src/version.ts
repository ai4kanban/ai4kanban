// The released version, baked into the built file.
//
// Do NOT hand-edit — it is stamped from the repo's root VERSION file by
// scripts/sync-version.mjs (the one number for the whole repo; see PUBLISHING.md). It is
// baked in because the built file is copied into installed projects away from every
// manifest, so `version` can answer with nothing fetched.
export const SKILL_VERSION = '0.6.1'
