# Verify a channel draft

- **Read independently**: read the named draft, `memory/writing.md` and only assigned files under `memory/writing/`. Do not read writing sessions, logs or prior reports.
- **Check applicable rules**: report each violation with its memory path, quoted rule, failing passage and needed correction. Respect each rule's scope; invent no standards.
- **Report only**: change no files and start no runs. If all applicable rules pass, reply exactly `PASS`; otherwise return the findings.

Do not save verification findings as writing memory; they are not user feedback.
