# 0.6.0

What each close or drop of this release left behind. This is a list of cards, not a
changelog — not every change goes through the board, so only a person can say what the
version changed.

## Closed 2026-08-14

What it was for — - [ ] UI优先（或者说看板优先）：通过Skill进行首次安装和配置的不可控因素太多，用户的主要操作应该通过GUI，Skill只作为后续可选项。 - [ ] 桌面端：考虑到人负责取舍而非执行，人应该可以在任意设备上提供决策，而不仅是web浏览器。 - [ ] CLI：与agent harness的交互、看板管理都封装在CLI中，UI后端通过CLI进行看板/agent操作，这使得Skill能够实现UI同样的功能。 实现上述功能。

Shipped — 23 cards, archived while naming `0.6.0`:

- #140 Schedule an action on a blocked card so it runs when the blocker is done (features)
- #167 Make akb the one way to run the board (skill)
- #168 Start, watch, stop and resume an agent run from the CLI (skill)
- #169 Let the board UI do its work through the CLI (features)
- #171 Make a board from the UI, with no command to type (features)
- #172 Walk a first-time user through setup in the UI (features)
- #173 Let the UI finish the setup steps that need an agent (features)
- #174 Add the coding agent skill later, from a button in the UI (features)
- #175 Open the board as a desktop app (features)
- #177 Make the board UI the way in, in the README and on the site (distribution)
- #178 Open and switch projects from the app, the way an IDE does (features)
- #180 Track each competitor as a feature checklist tied to your cards (distribution)
- #184 Mark the npm board UI deprecated on the registry (distribution)
- #185 Teach opening and switching projects where the app is (distribution)
- #192 Move the board script inside the CLI (skill)
- #194 Let the CLI print a board flow instead of running it (features)
- #195 Make akb the agent's manual, and shrink the skill to a note (skill)
- #196 Simplify install and update, and teach akb in the docs (skill)
- #207 Show which agents are installed on this machine in the picker (features)
- #208 Fix the board UI's production build (features)
- #210 Go back and forward in the desktop app with a swipe (features)
- #211 Drop the auto-refine switch — a refine follows the run that touched the card (features)
- #212 Find a card by typing in the rail (features)

Sent back with no release — nothing was still open.
