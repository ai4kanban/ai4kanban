# 0.6.1

What each close or drop of this release left behind. This is a list of cards, not a
changelog — not every change goes through the board, so only a person can say what the
version changed.

## Closed 2026-08-19

Shipped — 9 cards, archived while naming `0.6.1`:

- #77 Read the board's memory in the UI (features)
- #129 Read the project's memory in the UI (features)
- #130 Read a module's memory in the UI (features)
- #160 Run the board with an agent beyond Claude Code and Codex (features)
- #215 Bring the plain-Markdown mirror of the landing page back in line (distribution)
- #225 Run the board on DeepSeek Harness (dsh) (features)
- #226 Install the akb command from the desktop app (features)
- #230 Say on the setup strip when the setup run fails (features)
- #235 Publish a dsh install line that actually works (features)

Sent back with no release — nothing was still open.
