# Reusable video assets

Paths are under the board's assets folder. One line per asset: path, what it shows, what it suits, its limits, and the product change that makes it need recapturing.

`850/00-music.mp3`：CC0 lo-fi 音乐；适合平稳的产品演示铺底；123.167 秒，须在旁白下混音；批准的音乐选择变化时替换。
`850/00-click.mp3`：CC0 点击音；只适合脚本中的真实点击；0.097 秒；操作音选择变化时替换。
`850/00-select.mp3`：CC0 选择音；只适合脚本中的真实选项选择；0.040 秒；操作音选择变化时替换。
`850/00-confirm.mp3`：CC0 确认音；只适合脚本中的提交和启动反馈；0.290 秒；操作音选择变化时替换。
`850/00-logo-mark.svg`：仓库产品标志；适合口播标志字幕和片尾署名；`web/public/logo-mark.svg` 变化时替换。
`850/10-scriptwriter.png`：仓库角色图；适合 S10 署名；96×96 透明；角色图系列变化时替换。
`850/10-video-assets.png`：仓库角色图；适合 S10 署名；96×96 透明；角色图系列变化时替换。
`850/10-video-editor.png`：仓库角色图；适合 S10 署名；96×96 透明；角色图系列变化时替换。
`850/01-narration-en.wav`：Kokoro `af_heart` 的 S1 批准英文旁白；5.077 秒、24 kHz 单声道；批准文案变化时替换。
`850/01-narration-zh.wav`：Kokoro `zf_xiaobei` 的 S1 批准中文旁白；6.720 秒、24 kHz 单声道；批准文案变化时替换。
`850/02-narration-en.wav`：Kokoro `af_heart` 的 S2 批准英文旁白；3.093 秒、24 kHz 单声道；批准文案变化时替换。
`850/02-narration-zh.wav`：Kokoro `zf_xiaobei` 的 S2 批准中文旁白；3.968 秒、24 kHz 单声道；批准文案变化时替换。
`850/03-narration-en.wav`：Kokoro `af_heart` 的 S3 批准英文旁白；3.584 秒、24 kHz 单声道；批准文案变化时替换。
`850/03-narration-zh.wav`：Kokoro `zf_xiaobei` 的 S3 批准中文旁白；5.803 秒、24 kHz 单声道；批准文案变化时替换。
`850/04-narration-en.wav`：Kokoro `af_heart` 的 S4 批准英文旁白；3.477 秒、24 kHz 单声道；批准文案变化时替换。
`850/04-narration-zh.wav`：Kokoro `zf_xiaobei` 的 S4 批准中文旁白；7.125 秒、24 kHz 单声道；批准文案变化时替换。
`850/05-narration-en.wav`：Kokoro `af_heart` 的 S5 批准英文旁白；5.824 秒、24 kHz 单声道；批准文案变化时替换。
`850/05-narration-zh.wav`：Kokoro `zf_xiaobei` 的 S5 批准中文旁白；8.768 秒、24 kHz 单声道；批准文案变化时替换。
`850/06-narration-en.wav`：Kokoro `af_heart` 的 S6 批准英文旁白；4.459 秒、24 kHz 单声道；批准文案变化时替换。
`850/06-narration-zh.wav`：Kokoro `zf_xiaobei` 的 S6 批准中文旁白；7.936 秒、24 kHz 单声道；批准文案变化时替换。
`850/07-narration-en.wav`：Kokoro `af_heart` 的 S7 批准英文旁白；4.821 秒、24 kHz 单声道；批准文案变化时替换。
`850/07-narration-zh.wav`：Kokoro `zf_xiaobei` 的 S7 批准中文旁白；7.701 秒、24 kHz 单声道；批准文案变化时替换。
`850/08-narration-en.wav`：Kokoro `af_heart` 的 S8 批准英文旁白；3.776 秒、24 kHz 单声道；批准文案变化时替换。
`850/08-narration-zh.wav`：Kokoro `zf_xiaobei` 的 S8 批准中文旁白；9.408 秒、24 kHz 单声道；批准文案变化时替换。
`850/09-narration-en.wav`：Kokoro `af_heart` 的 S9 批准英文旁白；2.752 秒、24 kHz 单声道；批准文案变化时替换。
`850/09-narration-zh.wav`：Kokoro `zf_xiaobei` 的 S9 批准中文旁白；3.861 秒、24 kHz 单声道；批准文案变化时替换。
`850/10-narration-en.wav`：Kokoro `af_heart` 的 S10 批准英文旁白；2.645 秒、24 kHz 单声道；批准文案变化时替换。
`850/10-narration-zh.wav`：Kokoro `zf_xiaobei` 的 S10 批准中文旁白；4.288 秒、24 kHz 单声道；批准文案变化时替换。
