# Shipped

User-facing work that has shipped, one line each — a link to the published doc that
covers it, or a plain-words note.

- What belongs in `goal.md`, as advice the user can skip — setup links it when it asks for the goal: `docs/guides/what-makes-a-good-goal.md`.
- What each value of the goal's `reviewed:` field means, who writes it, and which one makes the board ask you for a goal: `docs/guides/daily-loop.md`.
- Both READMEs open on how to get `akb` and close on the two lines that update it, and teach the commands a person types rather than the board's bookkeeping: `README.md`, `README-zh.md`.
- Both READMEs' quick start now starts on downloading the board app, with what a Mac user has to click through on first open beside it and a picture of the guided first run; the `akb` command and `akb install` are step two, and the coding agent skill is its own optional section further down. No page says installing a board installs the skill — the setup prompt at `web/public/INSTALL_PROMPT.txt` and the npm page `cli/README.md` say the same: `README.md`, `README-zh.md`.
