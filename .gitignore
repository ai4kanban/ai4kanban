# Dogfooding symlink -> skill/ (avoids syncing edits across two paths)
.claude/
.agents/

# The skill copy the `ai4kanban` npm package ships — rebuilt by cli/scripts/bundle-skill.mjs
# on every publish, so it never belongs in git.
cli/skill/

# Local UI session registry: live agent sessions + their captured logs (task #12)
docs/kanban/.sessions.json
docs/kanban/.sessions/
# Archived proposals
docs/kanban/.archive/

keyword-research/
.env
social-posts/
.DS_Store
