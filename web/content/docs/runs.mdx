---
title: "Runs, and the tool behind them"
title_tag: "AI4Kanban runs — watch one, stop it, resume it, and pick what it runs on"
description: "A run is an agent working on its own: what to say to watch, stop or resume one, and how runtimes point a flow at a different agent or model."
lead: "A run is an agent working on its own — it survives a closed terminal, keeps its own log, and can be stopped. Here's how you drive one, and how you pick what it runs on."
last_updated: "September 4, 2026"
nav_label: "Runs and runtimes"
icon: "terminal"
---

Everything in [the daily loop](/docs/daily-loop) is a card. The rest of what the board app's
buttons do can be asked for just as plainly, and your agent runs the matching command — the
runs panel and `akb run` are the same list either way.

| You say | What happens |
| --- | --- |
| "what's running?" | the runs going now, and what ran lately |
| "show me that run's log" | its log, or `--follow` to watch it go |
| "stop it" | the run ends and the card comes back |
| "that run died — carry on" | it resumes the agent's own session, counted as a fresh run |
| "use Codex instead" / "switch to Opus" | the board's agent, model, reasoning effort |
| "check my setup works" | one small message through the setup, so a bad login turns up here rather than on your next card |
| "save my API key" | it hands **you** the command to type yourself |

A key is the one thing your agent hands back instead of running: a key it types lands in its
transcript and in your shell history, and the board never reads a saved key back.

When an ask can't run, the answer names the one line that fixes it — `akb install` for a project
with no board, `npm install -g ai4kanban@latest` for an `akb` that is behind, `akb agent test` for
an agent that isn't installed, `akb agent` for a key or provider that isn't set.

## Which tool each flow runs on

A **runtime** is a name the board holds — `default`, `cheap` — and every flow and spec skill runs
on one: the global one unless it names another. What each runtime spawns is the board's answer
too, in `docs/kanban/ui.config.json` beside the names, so every checkout of the repository runs
the same thing with nothing to set up per machine.

```bash
akb agent runtimes                     # the runtimes, and what each flow and agent is on
akb agent runtime add cheap            # name one
akb agent runtime for implement cheap  # point a flow — or a spec skill — at it
akb agent bind cheap codex             # what it runs as
akb agent runtime set cheap model gpt-5.1-codex
```

A runtime with nothing of its own runs `akb agent use`'s pick, which is the global runtime's
agent — so a new name works before anyone sets it. A runtime's settings start from that agent's
own block and override it key by key.

**Configuration → Runtimes** is the same thing with the answers on screen: one row per runtime,
opening onto the agent it runs and everything that agent is set to.

Which agent to put behind a runtime — what each of the eight reports back, what a run may touch,
and the settings each takes — is [What each coding agent can do](/docs/connectors).
