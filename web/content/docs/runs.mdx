---
title: "Runs, and the tool behind them"
title_tag: "AI4Kanban runs — watch one, stop it, resume it, and pick what it runs on"
description: "A run is an agent working on its own: what to say to watch, stop or resume one, and how each agent picks the coding tool and model it runs."
lead: "A run is an agent working on its own — it survives a closed terminal, keeps its own log, and can be stopped. Here's how you drive one, and how each agent picks what it runs on."
last_updated: "September 4, 2026"
nav_label: "Runs and agents"
icon: "terminal"
---

Everything in [the daily loop](/docs/daily-loop) is a card. The rest of what the board app's
buttons do can be asked for just as plainly, and your agent runs the matching command — the
runs panel and `akb run` are the same list either way.

| You say | What happens |
| --- | --- |
| "what's running?" | the runs going now, and what ran lately |
| "show me that run's log" | its log, or `--follow` to watch it go |
| "stop it" | the run ends and the card comes back |
| "that run died — carry on" | it resumes the agent's own session, counted as a fresh run |
| "use Codex instead" / "switch to Opus" | the board's agent, model, reasoning effort |
| "check my setup works" | one small message through the setup, so a bad login turns up here rather than on your next card |
| "save my API key" | it hands **you** the command to type yourself |

A key is the one thing your agent hands back instead of running: a key it types lands in its
transcript and in your shell history, and the board never reads a saved key back.

When an ask can't run, the answer names the one line that fixes it — `akb install` for a project
with no board, `npm install -g ai4kanban@latest` for an `akb` that is behind, `akb agent test` for
an agent that isn't installed, `akb agent` for a key or provider that isn't set.

## Where a run's record is kept

A run's record and its log are this machine's, not the repository's — and they live outside it,
in `~/.ai4kanban/projects/<project>/`, one folder per board. The chats, the mockups and the
comment batches are there too. The project keeps only what it commits, so nothing has to be
kept out of git by hand.

```
~/.ai4kanban/projects/my-app-3f9c1b2a04/
  board.json      which board this folder is for
  sessions.json   what is running, and what ran lately
  sessions/       one log per run
  chats/          the conversations held on this machine
  mockups/        the drawings a card points at
  comments/       the comment batches waiting for a polish
```

Two checkouts of one repository are two folders, and so are two boards in one repository: runs
started in one are never listed by the other.

**Move or rename the project and it starts fresh.** The old folder stays where it is, with the
old path in its own `board.json`, so bringing the history across is one move you make yourself:

```bash
grep -l /old/path ~/.ai4kanban/projects/*/board.json   # which folder was the old one
mv ~/.ai4kanban/projects/<old>/sessions* ~/.ai4kanban/projects/<new>/
mv ~/.ai4kanban/projects/<old>/chats ~/.ai4kanban/projects/<new>/
```

Open the board once at the new path first, so the new folder is there to move into. Leave
`board.json` behind — each folder's own says which board it is for.

### Upgrading from a version that kept them in the project

Nothing is moved for you, and nothing you already have is read, changed or deleted. The new
folder starts empty: the runs it lists are the runs you start after the switch, and a chat from
before the switch cannot be carried on.

1. Finish what is in flight on the old version — the runs going, and the review and landing of
   each delivery they belong to.
2. Stop everything still running it: the app, any board server, any watcher.
3. Switch the command and the app together. Don't run the two versions side by side.

What the old version left in `docs/kanban/` — `.sessions.json`, `.sessions/`, `.chats/`,
`.mockups/`, `.comments/` and the lock folders — stays exactly where it is, still ignored by
git and still left alone by import and export. Delete it yourself once you are sure you are
done with that history; the lines in `docs/kanban/.gitignore` that cover it can go with it.

## Which runtime each agent runs on

Every run belongs to one agent: a flow is run by the role that owns it — planner, builder,
reviewer, or writer on a marketing board — and a spec run by the specialist it names. Each of them
names a **runtime**, which is the whole answer to what that run runs as: the coding tool, the
provider, the endpoint, the key, the model id, the reasoning level and any extra arguments. So the
planner can think on a stronger model than the builder writes with, and two agents on the same
tool can sit on two different gateways.

```bash
akb agent                                    # every runtime, then every agent and the row it runs
akb agent runtime add "My gateway" claude-code
akb agent set --runtime my_gateway baseUrl https://my-gateway.example.com
akb agent bind builder my_gateway            # give one agent a runtime of its own
```

The first row is **Global default** — what an agent naming no runtime runs. It is the default
because it is first, not because anything marks it, and it can be neither renamed nor deleted.

A runtime's SHAPE travels with the repository, in `docs/kanban/ui.config.json`, so every checkout
runs each agent as the same thing. Its **API key** does not: it is one line per runtime in
`docs/kanban/.env`, which git does not carry. Clone the repo somewhere else and every runtime is
there, each waiting for its own key.

On screen, **Configuration → Agents** is where each agent picks its runtime, and
**Configuration → Runtimes** opens each coding tool onto how to reach it and a **Test** that says
whether it answers. Rows themselves are added, renamed and deleted with `akb agent runtime`.

Which tool to put behind an agent — what each of the eight reports back, what a run may touch,
and the settings each takes — is [What each coding agent can do](/docs/connectors).
