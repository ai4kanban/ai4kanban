// Structure for the landing page — everything that *isn't* words.
//
// The page's own sections keep their structure inline (the icon per row, the
// memory tree, the agent marks), so what's left here is the one constant every
// page links to.

export const GITHUB_URL = "https://github.com/ai4kanban/ai4kanban";
